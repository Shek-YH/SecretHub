# R17 Reliability, Persistence, Links and Quick Copy Evidence

- Export Preview and Export now catch native IPC/filesystem errors and render an alert in the dialog instead of leaving the user with no visible response.
- Edit form loads stored metadata and description; leaving Value blank calls the metadata-only update path, preserving the encrypted payload without returning the old plaintext to WebView.
- Provider API-key links and URL values use a Rust `open` bridge for allowlisted HTTPS system-browser opening in Tauri; non-Tauri browser mode uses `window.open`.
- Detail actions provide API-key-only copy and full-form copy. Full-form formatting and clipboard cleanup remain native; neither action returns plaintext to the WebView.
- `cargo test --workspace`: exit 0; Core includes metadata-preservation coverage.
- `cargo clippy --workspace --all-targets -- -D warnings`: exit 0.
- Desktop `npm run typecheck`, `npm test -- --run`, `npm run build`, official-registry `npm audit --audit-level=high`: exit 0; 7/7 tests pass.
- MCP tests, typecheck, stdio smoke and official-registry audit: exit 0; 4/4 tests pass and 0 vulnerabilities.
- Golden E2E: exit 0; export preview path and updated Add Secret type/API KEY flow pass.
- Security scan: exit 0; no tracked `.env` or sensitive sentinel.
- `npx tauri build --debug`: exit 0; latest x64 exe, MSI and NSIS artifacts regenerated.

Manual Windows installer interaction and real provider API calls remain separate acceptance gates.
