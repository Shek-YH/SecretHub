# R19 Request URL and Profile Lifecycle Evidence

- API KEY forms persist model ID/model Env Key plus request URL/request URL Env Key as value-free metadata; export includes project-useful API key, model and request URL Env pairs.
- `.env` keeps one blank line between credential groups. Remarks are emitted as `#` comments; SecretHub management fields are excluded.
- Copy all reuses the same Env projection, so it includes project Env content and remarks only.
- Multi-selection “加入组合” opens a name/description prompt and stores a profile of Secret IDs. The Profiles page reads, edits, deletes and applies profiles.
- Profile application resolves Secret IDs through the current Vault payloads, so updating a Secret automatically affects the next profile export without copying old values into the profile.
- `cargo test --workspace`: exit 0; 7 Core tests, 6 exporter tests and 3 provider tests pass among the workspace suite.
- `cargo clippy --workspace --all-targets -- -D warnings`: exit 0.
- Desktop `npm run typecheck`, `npm test -- --run`, `npm run build`: exit 0; 8/8 tests pass.
- MCP tests/typecheck/smoke/official audit: exit 0; 4/4 tests pass and 0 vulnerabilities.
- Security scan: exit 0.
- `npx tauri build --debug`: exit 0; x64 exe, MSI and NSIS regenerated.
