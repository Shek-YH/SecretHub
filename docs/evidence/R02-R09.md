# R02-R09 Evidence

- `cargo test --workspace`: exit 0; crypto, redaction, SQLite, vault restart/unlock, CRUD, exporter, profile, provider, backup and Tauri crate tests pass.
- `cargo check --workspace`: exit 0.
- `npm run typecheck && npm test && npm run build`: exit 0; 2 Vitest files / 2 tests pass and Vite bundle is produced.
- `npx tauri build --debug`: exit 0; MSI and NSIS bundles are produced under `target/debug/bundle`.
- Playwright wrapper E2E: exit 0; default Chinese, English switch, form add, multi-select and masked export preview pass; screenshots are in this directory.
- `powershell -ExecutionPolicy Bypass -File tests/security/security_scan.ps1`: exit 0; source/test scan has no sensitive sentinel/private-key marker and Git tracks no `.env`/backup export.
- Provider SSRF tests: exit 0; only fixed HTTPS provider origins are accepted.
- Auto-lock policy tests: exit 0; Tauri command activity checks enforce idle expiry and settings update replaces the policy.
- Native process launch smoke: exit 0; `target/debug/secrethub-desktop.exe` stayed alive for 3 seconds and was then closed by the test harness.
- Final `npx tauri build --debug`: exit 0; x64 MSI and NSIS installers regenerated after the latest CRUD, conflict and auto-lock changes.
- Final `cargo fmt --all -- --check`: exit 0.
- Final `cargo clippy --workspace --all-targets -- -D warnings`: exit 0.
- Backup path checkpoints SQLite WAL before encrypting the backup; Copy schedules conditional clipboard clearing using a value hash.

Automated evidence uses synthetic fixtures only. Installer launch, first-run Windows interaction and real-directory export remain manual acceptance gates.
