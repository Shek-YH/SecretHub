# R16 Credential Type Routing Evidence

- Add Secret now renders a type router with `API KEY`, `TOKEN`, `URL`, `Password` and `Text` options.
- API KEY transitions to the existing Provider/model catalog; TOKEN transitions directly to a `TOKEN / TOKEN` form and does not render the model catalog.
- URL uses a URL input and preserves the open-url action; Password and Text use their own value input modes.
- `npm test -- --run src/App.test.tsx`: exit 0, 3/3 tests pass.
- Golden E2E: exit 0 after updating the flow to click `API KEY` before choosing OpenAI.
- `npm run typecheck`, `npm test -- --run`, `npm run build`: exit 0.
- `npx tauri build --debug`: latest x64 executable, MSI and NSIS artifacts regenerated under `target/debug`.

The current implementation is self-verified. Native installer interaction and real provider calls remain separate manual/user-only acceptance gates.
