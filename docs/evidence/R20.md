# R20 Profile Detail Draft Editor Evidence

- Profile list “Edit” now opens a dedicated detail dialog with editable name/description and all current member credentials.
- Removing a credential only updates draft Secret IDs and never deletes the Vault credential.
- “Add credential” opens a nested full-vault picker with checkboxes. Its confirmation returns IDs to the detail draft only.
- The detail dialog’s final “Save profile” is the only point that calls the existing ID-backed profile save path. Profile application therefore resolves current encrypted payloads after a credential update.
- Desktop `npm run typecheck`, `npm test -- --run`, `npm run build`: exit 0; 9/9 tests pass.
- Rust workspace and Clippy remain green from the preceding R19 checkpoint; this change is UI-only apart from using the existing profile save/delete commands.
- The latest Tauri package rebuild is performed after this checkpoint; native installer interaction remains a manual user gate.
