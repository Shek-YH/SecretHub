{
  "name": "secrethub",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix apps/desktop run dev",
    "build": "npm --prefix apps/desktop run build",
    "test": "npm --prefix apps/desktop run test",
    "typecheck": "npm --prefix apps/desktop run typecheck",
    "mcp:test": "npm --prefix apps/mcp run test",
    "mcp:build": "npm --prefix apps/mcp run build",
    "mcp:smoke": "npm --prefix apps/mcp run smoke"
  }
}
